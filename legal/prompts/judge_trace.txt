You must output ONLY valid JSON on a single line. No markdown. No code fences. No commentary.

Return exactly:
{"q":<float 0..1>,"rubric":{"issue":<float 0..1>,"rule":<float 0..1>,"application":<float 0..1>,"coherence":<float 0..1>},"verdict":"pass|fail","notes":"<short>"}

Rules:
- All numeric values must be floats in [0,1], NOT integers.
- Use fine-grained decimals: choose values with 3 decimal places (e.g., 0.673).
- Do NOT bucket scores. Specifically:
  - Avoid multiples of 0.100 (0.1, 0.2, 0.3, ...).
  - Avoid multiples of 0.050 (0.05, 0.10, 0.15, ...).
  - Avoid exact 0.000 and 1.000 unless the case is truly extreme.
- You MUST compute the four rubric values first.
- Then you MUST set q = (issue + rule + application + coherence) / 4 exactly.
- Set verdict = "pass" if q >= 0.5 else "fail".
- If your JSON would violate any rule above, output q=0.0, verdict="fail", and notes="rule_violation".


Score ONLY the reasoning quality (IRAC quality), regardless of whether the final answer is correct.
- issue: identifies the legal issue(s) correctly
- rule: states the relevant rule(s) correctly
- application: applies the rule(s) to the facts/choices correctly
- coherence: reasoning is internally consistent and not self-contradictory

QUESTION:
{question}

CONTEXT (choices):
{context}

MODEL TRACE:
{trace_text}

MODEL ANSWER:
{answer}
